{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --host",
    "build": "vite build",
    "preview": "vite preview --open --host",
    "deploy": "surge ./dist amira-portfolio.surge.sh"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
